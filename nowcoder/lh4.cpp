#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    // M个玩家，2种鲜花，每种采集N朵
    int m, n;
    cin >> m >> n;
    vector<pair<int, int>> p(m);
    for (int i = 0; i < m; i++) { cin >> p[i].first >> p[i].second; }

    return 0;
}