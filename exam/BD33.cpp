#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define show(x, idx, n)                                                                                                                                        \
    for (int i = idx; i < n; i++) cout << x[i] << ' ';                                                                                                         \
    cout << endl;

int

    int
    main() {
    //
    int n, q;
    string s;
    cin >> n >> q;
    while (q--) {
        cin >> s;
        res = 0;
        fun(s, n);
        cout << res << endl;
        cout << s << endl;
    }
    return 0;
}