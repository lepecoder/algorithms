#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++) cin >> arr[i];
    // 好序列 a[1] = n-1 且 n>1
    // 完美序列  可以连续分割成多个好序列
    // 一个序列a，存在多少个子序列是完美序列
    // 好序列只和第一个元素有关

    return 0;
}