#include <bits/stdc++.h>
using namespace std;
class Solution {
  public:
    int divide(int a, int b) {
        bool posi = true;
        if (a < 0) {
            a = -a;
            posi = ~posi;
        }
        if (b < 0) {
            b = -b;
            posi = ~posi;
        }
        int ret = calc(a, b);
        if () }
};
int main() {
    //
    return 0;
}